{% extends 'base.html.twig' %}

{% block title %}Listes des formateurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

       <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Nom</th>
                    <th scope="col">Prenom</th>
                    <th scope="col">Spécialité</th>
                    <th></th>
                    {# <th scope="col">Telephone</th>
                    <th scope="col">Mail</th>  #}
                    
                </tr>
            </thead>
            <tbody>
                {% for formateur in formateurs %}
                <tr>
                    <td scope="row"><a href="{{ path('formateur_show', {'id': formateur.id}) }}">{{ formateur.nom }}</a></td>
                    <td>{{ formateur.prenom }}</td>
                    <td>{{ formateur.specialite }}</td>
                                                            {% if is_granted("ROLE_ADMIN")%} 
                    
                <td><a class="btn btn-secondary" href="{{ path('formateur_edit', {'id': formateur.id}) }}">Modifier</a></td>   
                <td><a class="btn btn-secondary" href="{{ path('formateur_delete', {'id': formateur.id}) }}">X</a> 
                {% endif %} </div></td>
                </tr>
            </tbody>
                {% endfor %}


{% endblock %}
