{% extends 'base.html.twig' %}

{% block title %}Listes des formateurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

       <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Intitule</th>
                    <th scope="col">Catégorie</th>
                    <th></th>
                    <th></th>

                    
                </tr>
            </thead>
            <tbody>
                {% for module in modules %}
                <tr>
                    <td scope="row">{{ module.intitule }}</td>
                    <td>{{ module.theme.intitule }}</td>
                                                            {% if is_granted("ROLE_ADMIN")%} 
                    
                <td><a class="btn btn-secondary" href="{{ path('module_edit', {'id': module.id}) }}">Modifier</a></td>   
                <td><a class="btn btn-secondary" href="{{ path('module_delete', {'id': module.id}) }}">X</a> 
                {% endif %} </div></td>
                </tr>
            </tbody>
                {% endfor %}


{% endblock %}
