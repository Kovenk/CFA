{% extends 'base.html.twig' %}

{% block title %}Listes des formateurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

       <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Nom</th>
                    <th scope="col">Prenom</th>
                    <th scope="col">Date de Naissance</th>
                    <th scope="col">Telephone</th>
                    <th scope="col">Mail</th> 
                    
                </tr>
            </thead>
            <tbody>
                {% for formateur in formateurs %}
                <tr>
                    <td scope="row"><a href="{{ path('formateur_show', {'id': formateur.id}) }}">{{ formateur.nom }}</a></td>
                    <td>{{ formateur.prenom }}</td>
                    <td>{{ formateur.dateNaissance | date('d/m/Y') }}</td>
                    <td>{{ formateur.telephone }}</td>
                    <td>{{ formateur.mail }} </td>
                </tr>
            </tbody>
                {% endfor %}


{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Listes des formateurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

       <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Ititulé de la session</th>
                    <th scope="col">Date de Debut</th>
                    <th scope="col">Date de Fin</th>
                    <th scope="col">Place Totale</th>
                    <th scope="col">Nombre d'inscrits</th> 
                    
                </tr>
            </thead>
            <tbody>
                {% for formateur in formateurs %}
                <tr>
                    <td scope="row"><a href="{{ path('formateur_show', {'id': formateur.id}) }}">{{ formateur.nom }}</a></td>
                    <td>{{ formateur.prenom }}</td>
                    <td>{{ formateur.dateNaissance | date('d/m/Y') }}</td>
                    <td>{{ formateur.telephone }}</td>
                    <td>{{ formateur.mail }} </td>
                </tr>
            </tbody>
                {% endfor %}


{% endblock %}
